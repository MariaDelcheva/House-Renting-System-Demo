pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    if (isUnix()) {
                        // Unix-specific steps
                    } else if (isWindows()) {
                        // Windows-specific steps
                    }
                }
            }
        }
    }
}
